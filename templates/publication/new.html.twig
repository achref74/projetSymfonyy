{% extends 'frontBase.html.twig' %}

{% block title %}New Publication{% endblock %}

{% block body %}
  <div class="row">
        <div class="col-lg-9 align-self-center">
          <div class="row">
            <div class="col-lg-12">

    <h1>Create new Publication</h1>

    {{ include('publication/_form.html.twig') }}
      </div>
            </div>
          </div>
        </div>

    <a href="{{ path('app_publication_index', {'idUser': idUser, 'idForum': idForum, 'idformation': idformation }) }}">back to list</a>
    
{% endblock %}
