{% extends 'backBase.html.twig' %}

{% block title %}Reponse index{% endblock %}

{% block body %}
    <h1>Reponse index</h1>

    <table class="table">
        <thead>
            <tr>
                
                <th>IdUser</th>
                <th>Description</th>
                <th>Reclamation concernée</th>
                <th>DateReponse</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for reponse in reponses %}
            <tr>
                
                <td>{{ reponse.user.idUser}}</td>
                <td>{{ reponse.description }}</td>
                <td>{{reponse.reclamation.description}}</td>

                <td>{{ reponse.dateReponse ? reponse.dateReponse|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_reponse_show', {'id': reponse.id}) }}">show</a>
                                        {{ include('reponse/_delete_form.html.twig') }}

                    <a href="{{ path('app_reponse_edit', {'id': reponse.id}) }}">edit</a>
                    <a href="{{ path('app_reponse_new', {'reclamationId': reponse.reclamation.id}) }}" class="btn btn-success">Create new</a>

                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}