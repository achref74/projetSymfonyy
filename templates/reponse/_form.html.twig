{{ form_start(form, { 'attr': { 'novalidate': 'novalidate' } }) }}
<div class="count-title">
    {{ form_label(form.description, null, {'attr': {'class': 'white-label'}}) }}  <!-- Description label with white class -->
    {{ form_widget(form.description) }} 
</div>
<div class="count-digit">
    {{ form_errors(form.description) }}  <!-- Description input -->
</div>


    <button class="button">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
