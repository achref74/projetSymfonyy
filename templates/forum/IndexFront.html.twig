{% extends 'frontBase.html.twig' %}

{% block title %}Forum index{% endblock %}

{% block body %}
    <h1>Forum index</h1>

    <div class="row">
    {% for forum in forums %}
    <div class="col-lg-3" style="margin-right: 15px;">
        <h6 style="color: white;">Titre {{ forum.titre }}</h6><br>
        <span style="color: white;">{{ forum.datecreation ? forum.datecreation|date('Y-m-d H:i:s') : '' }}</span><br>
        <span style="color: white;">{{ forum.description }}</span><br>
        <span>
             <!-- Add Publication Button -->

    
    {% else %}
    <p>No records found</p>
    {% endfor %}
</div>

    
{% endblock %}
