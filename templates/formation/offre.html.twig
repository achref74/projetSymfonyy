{% extends 'base1.html.twig' %}

{% block content %}
<!-- partial -->
<div class="main-panel">
  <div class="content-wrapper">
    <div class="page-header">
      <h3 class="page-title"> Tableaux Des Offres</h3>
      <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
          
          
        </ol>
      </nav>
    </div>
    <div class="row">
      <div class="col-lg-6 grid-margin stretch-card">
        <div class="card">
          <div class="card-body">
            <h4 class="card-title">Offres</h4>
          
            <div class="table-responsive">
              <table class="table">
                <thead>
                  <tr>
                    <th>Description</th>
                    <th>Prix</th>
                    <th>Date Début</th>
                    <th>Date Fin</th>
                  </tr>
                </thead>
                <tbody>
                  {% for offre in offres %}
                  <tr>
                    <td>{{ offre.description }}</td>
                    <td>{{ offre.prixOffre }}</td>
                    <td>{{ offre.dateD|date("d/m/Y") }}</td>
                    <td>{{ offre.dateF|date("d/m/Y") }}</td>
                  </tr>
                  {% endfor %}
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
{% endblock %}
