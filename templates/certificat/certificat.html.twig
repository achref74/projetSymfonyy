{# 
<!DOCTYPE html>
<html>
<head>
    <title>Certificat Information</title>
</head>
<body>
    <h1>Certificat Information</h1>
    <p><strong>Title:</strong> {{ certificat.titre }}</p>
    <p><strong>Description:</strong> {{ certificat.description }}</p>
    <p><strong>Date Obtention:</strong> {{ certificat.dateObtention|date('Y-m-d') }}</p>
    <!-- Add more information as needed -->
</body>
</html>  #}

<html>
    <head>
        <style type='text/css'>
            body, html {
                margin: 0;
                padding: 0;
            }
            body {
                color: black;
                display: table;
                font-family: Georgia, serif;
                font-size: 24px;
                text-align: center;
            }
            .container {
                border: 20px solid tan;
                width: 1500px;
                height: 600px;
                display: table-cell;
                vertical-align: middle;
            }
            .logo {
                color: tan;
            }

            .marquee {
                color: tan;
                font-size: 48px;
                margin: 20px;
            }
            .assignment {
                margin: 20px;
            }
            .person {
                border-bottom: 2px solid black;
                font-size: 32px;
                font-style: italic;
                margin: 20px auto;
                width: 400px;
            }
            .reason {
                margin: 20px;
            }
        </style>
    </head>
    <body>
        <div class="container">
            <div class="logo">
                Artisan
            </div>

            <div class="marquee">
                Certificate of Completion
            </div>

            <div class="assignment">
                This certificate is presented to
            </div>

            <div class="person">
                user
            </div>

            <div class="reason">
                For completing the courses of {{ certificat.titre }}
            </div>
            <div class="reason">
                 {{ certificat.dateObtention|date('Y-m-d') }}
            </div>
        </div>
    </body>
</html>